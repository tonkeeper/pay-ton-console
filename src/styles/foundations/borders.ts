import {borderRadius} from "./border-radius";

export const borders = {
    none: 'none',
    sm: `1px solid ${borderRadius.sm}`,
    md: `1px solid ${borderRadius.md}`,
    lg: `1px solid ${borderRadius.lg}`
}
